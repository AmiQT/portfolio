---
import Layout from "../../layouts/Layout.astro";
import IdeActivityBar from "../../components/software-dev/IdeActivityBar.astro";
import IdeSidebar from "../../components/software-dev/IdeSidebar.astro";
import MobileNav from "../../components/software-dev/MobileNav.astro";
import NotebookCell from "../../components/software-dev/NotebookCell.astro";

// Project Data
const project = {
    title: "StyleHub - Fashion E-Commerce Platform",
    type: "Web",
    description:
        "A modern, responsive e-commerce website built with React, Node.js, and PostgreSQL. Features a complete shopping experience with user authentication, product management, shopping cart, and order processing.",
    image: "assets/stylehub.png",
    liveUrl: "https://e-commerce-coral-five-81.vercel.app/",
    duration: "3-6 months",
    role: "Full-stack Developer",
};

const technologies = [
    { name: "React", color: "blue" },
    { name: "Node.js", color: "green" },
    { name: "PostgreSQL", color: "purple" },
    { name: "Tailwind CSS", color: "cyan" },
    { name: "Express", color: "gray" },
    { name: "JWT", color: "orange" },
];

const features = [
    {
        title: "Shopping Experience",
        icon: "🛒",
        desc: "Product catalog with categories and search, shopping cart with real-time updates, wishlist functionality.",
    },
    {
        title: "User Management",
        icon: "👤",
        desc: "User registration and authentication, user profiles and preferences, order history and tracking.",
    },
    {
        title: "Checkout & Payments",
        icon: "💳",
        desc: "Multi-step checkout process, multiple payment methods (Stripe, PayPal), shipping options.",
    },
    {
        title: "Responsive Design",
        icon: "📱",
        desc: "Mobile-first responsive design, touch-friendly interface, PWA features.",
    },
    {
        title: "Admin Features",
        icon: "📊",
        desc: "Product and category management, order management and status updates, analytics dashboard.",
    },
    {
        title: "Security",
        icon: "🔒",
        desc: "JWT authentication, password hashing, rate limiting, CORS protection.",
    },
];

const techStack = {
    frontend: [
        "React 18 with Vite",
        "Tailwind CSS",
        "React Router",
        "React Hook Form",
    ],
    backend: [
        "Node.js with Express",
        "PostgreSQL database",
        "JWT authentication",
        "RESTful API",
    ],
    deployment: [
        "Frontend: Vercel",
        "Backend: Render (PaaS)",
        "Database: Render PostgreSQL",
    ],
};

// Files for sidebar
const files = [
    {
        name: "stylehub.tsx",
        active: true,
        icon: "🛒",
        href: import.meta.env.BASE_URL + "projects/stylehub",
    },
    {
        name: "projects.json",
        active: false,
        icon: "📦",
        href: import.meta.env.BASE_URL + "role-software-developer/projects",
    },
    {
        name: "home-software-developer.ipynb",
        active: false,
        icon: "📝",
        href: import.meta.env.BASE_URL + "home-software-developer/",
    },
];
---

<Layout
    title="StyleHub - Fashion E-Commerce Website | Noor Azami"
    description={project.description}
>
    <div
        class="flex h-screen bg-white dark:bg-[#0d1117] text-gray-900 dark:text-gray-300 overflow-hidden font-sans transition-colors duration-300"
    >
        <IdeActivityBar />
        <IdeSidebar files={files} />

        <!-- Main Editor Area -->
        <main
            class="flex-1 flex flex-col min-w-0 bg-white dark:bg-[#0d1117] transition-colors duration-300"
        >
            <!-- Tab Bar -->
            <div
                class="flex bg-gray-100 dark:bg-[#010409] border-b border-gray-200 dark:border-gray-800 overflow-x-auto scrollbar-hide transition-colors duration-300"
            >
                <div
                    class="flex items-center gap-2 px-4 py-2 bg-white dark:bg-[#0d1117] border-t-2 border-orange-500 text-gray-900 dark:text-white text-sm min-w-fit cursor-pointer transition-colors duration-300"
                >
                    <span>🛒</span>
                    <span>stylehub.tsx</span>
                    <span
                        class="ml-2 text-gray-500 hover:text-gray-900 dark:hover:text-white cursor-pointer"
                        title="Close Editor">×</span
                    >
                </div>
                <a
                    href={import.meta.env.BASE_URL +
                        "role-software-developer/projects"}
                    class="flex items-center gap-2 px-4 py-2 text-gray-500 hover:bg-gray-200 dark:hover:bg-[#161b22] cursor-pointer text-sm min-w-fit transition-colors duration-300"
                >
                    <span>📦</span>
                    <span>projects.json</span>
                </a>
            </div>

            <!-- Breadcrumbs -->
            <div
                class="flex items-center gap-2 px-4 py-1 bg-white dark:bg-[#0d1117] border-b border-gray-200 dark:border-gray-800 text-xs text-gray-500 transition-colors duration-300"
            >
                <span>src</span>
                <span>›</span>
                <span>projects</span>
                <span>›</span>
                <span>stylehub.tsx</span>
                <div class="ml-auto flex gap-2">
                    <span
                        class="px-2 py-0.5 bg-blue-100 dark:bg-blue-900/30 text-blue-700 dark:text-blue-300 rounded text-xs"
                        >Web App</span
                    >
                </div>
            </div>

            <!-- Content Area -->
            <div
                class="flex-1 overflow-y-auto p-4 md:p-8 scroll-smooth"
                id="project-content"
            >
                <!-- Cell 1: Project Header -->
                <NotebookCell
                    cellNumber={1}
                    code={`// Project: StyleHub E-Commerce\nimport { Project, TechStack } from '@portfolio/projects';\n\nconst stylehub = new Project({\n  name: "${project.title}",\n  type: "${project.type}",\n  duration: "${project.duration}",\n  role: "${project.role}"\n});\n\nstylehub.init();`}
                >
                    <section
                        class="relative flex flex-col items-center justify-center text-center py-12 md:py-16 bg-gradient-to-br from-pink-900 to-slate-900 rounded-lg overflow-hidden border border-pink-500/30 shadow-2xl"
                    >
                        <div class="z-10 px-4">
                            <div class="flex justify-center mb-4">
                                <span
                                    class="px-4 py-1 bg-blue-500/20 border border-blue-500/30 rounded-full text-sm text-blue-300"
                                    >Web Development</span
                                >
                            </div>
                            <h1
                                class="text-3xl md:text-4xl font-bold mb-4 leading-tight text-white drop-shadow-lg"
                            >
                                {project.title}
                            </h1>
                            <p
                                class="text-base md:text-lg font-medium mb-6 max-w-2xl mx-auto leading-relaxed text-gray-200"
                            >
                                {project.description}
                            </p>
                            <div
                                class="flex flex-wrap justify-center gap-2 mb-6"
                            >
                                {
                                    technologies.map((tech) => (
                                        <span class="px-3 py-1 bg-black/30 backdrop-blur-md border border-white/20 rounded-full text-xs text-white">
                                            {tech.name}
                                        </span>
                                    ))
                                }
                            </div>
                            <a
                                href={project.liveUrl}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="inline-flex items-center gap-2 px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white rounded-lg font-medium transition-colors"
                            >
                                <svg
                                    class="w-5 h-5"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                                    ></path>
                                </svg>
                                View Live Project
                            </a>
                        </div>
                    </section>
                </NotebookCell>

                <!-- Cell 2: Project Image -->
                <NotebookCell
                    cellNumber={2}
                    code={`// Render project preview\nconst preview = await stylehub.getPreview();\npreview.display({ quality: 'high' });`}
                >
                    <div
                        class="rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700 shadow-xl"
                    >
                        <img
                            src={import.meta.env.BASE_URL + project.image}
                            alt={project.title}
                            class="w-full h-auto object-cover"
                            loading="lazy"
                        />
                    </div>
                </NotebookCell>

                <!-- Cell 3: Key Features -->
                <NotebookCell
                    cellNumber={3}
                    code={`// Load and display features\nconst features = stylehub.getFeatures();\nfeatures.forEach(f => console.log(\`✓ \${f.title}\`));`}
                >
                    <div>
                        <h2
                            class="text-2xl font-bold text-gray-900 dark:text-white text-center mb-6"
                        >
                            Key Features
                        </h2>
                        <div
                            class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4"
                        >
                            {
                                features.map((feature) => (
                                    <div class="bg-white dark:bg-[#161b22] p-5 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-blue-500/50 transition-all">
                                        <div class="flex items-start gap-3">
                                            <div class="w-10 h-10 bg-blue-500/10 dark:bg-blue-500/20 rounded-lg flex items-center justify-center flex-shrink-0">
                                                <span class="text-xl">
                                                    {feature.icon}
                                                </span>
                                            </div>
                                            <div>
                                                <h3 class="font-semibold text-gray-900 dark:text-white mb-1">
                                                    {feature.title}
                                                </h3>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">
                                                    {feature.desc}
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                ))
                            }
                        </div>
                    </div>
                </NotebookCell>

                <!-- Cell 4: Tech Stack -->
                <NotebookCell
                    cellNumber={4}
                    code={`// Display technology stack\nconsole.table(stylehub.techStack);`}
                >
                    <div>
                        <h2
                            class="text-2xl font-bold text-gray-900 dark:text-white text-center mb-6"
                        >
                            Tech Stack
                        </h2>
                        <div class="grid grid-cols-1 md:grid-cols-3 gap-4">
                            <div
                                class="bg-white dark:bg-[#161b22] p-5 rounded-lg border border-gray-200 dark:border-gray-700"
                            >
                                <h3
                                    class="font-bold text-gray-900 dark:text-white mb-3 flex items-center gap-2"
                                >
                                    <span>🎨</span> Frontend
                                </h3>
                                <ul class="space-y-2 text-sm">
                                    {
                                        techStack.frontend.map((item) => (
                                            <li class="flex items-center gap-2 text-gray-600 dark:text-gray-400">
                                                <span class="text-green-500">
                                                    ✓
                                                </span>{" "}
                                                {item}
                                            </li>
                                        ))
                                    }
                                </ul>
                            </div>
                            <div
                                class="bg-white dark:bg-[#161b22] p-5 rounded-lg border border-gray-200 dark:border-gray-700"
                            >
                                <h3
                                    class="font-bold text-gray-900 dark:text-white mb-3 flex items-center gap-2"
                                >
                                    <span>⚙️</span> Backend
                                </h3>
                                <ul class="space-y-2 text-sm">
                                    {
                                        techStack.backend.map((item) => (
                                            <li class="flex items-center gap-2 text-gray-600 dark:text-gray-400">
                                                <span class="text-green-500">
                                                    ✓
                                                </span>{" "}
                                                {item}
                                            </li>
                                        ))
                                    }
                                </ul>
                            </div>
                            <div
                                class="bg-white dark:bg-[#161b22] p-5 rounded-lg border border-gray-200 dark:border-gray-700"
                            >
                                <h3
                                    class="font-bold text-gray-900 dark:text-white mb-3 flex items-center gap-2"
                                >
                                    <span>🚀</span> Deployment
                                </h3>
                                <ul class="space-y-2 text-sm">
                                    {
                                        techStack.deployment.map((item) => (
                                            <li class="flex items-center gap-2 text-gray-600 dark:text-gray-400">
                                                <span class="text-green-500">
                                                    ✓
                                                </span>{" "}
                                                {item}
                                            </li>
                                        ))
                                    }
                                </ul>
                            </div>
                        </div>
                    </div>
                </NotebookCell>

                <!-- Back to Projects -->
                <div class="mt-8 text-center">
                    <a
                        href={import.meta.env.BASE_URL +
                            "role-software-developer/projects"}
                        class="inline-flex items-center gap-2 px-6 py-3 bg-gray-100 dark:bg-[#161b22] border border-gray-200 dark:border-gray-700 rounded-lg text-sm font-medium text-gray-700 dark:text-gray-300 hover:border-blue-500/50 hover:text-blue-600 dark:hover:text-blue-400 transition-all"
                    >
                        <span>←</span> Back to All Projects
                    </a>
                </div>
            </div>
        </main>

        <MobileNav files={files} />
    </div>
</Layout>

<script>
    // Theme Toggle Logic
    const themeBtn = document.getElementById("theme-toggle-btn");
    const sunIcon = document.getElementById("sun-icon");
    const moonIcon = document.getElementById("moon-icon");
    const html = document.documentElement;

    const savedTheme = localStorage.getItem("theme");
    const systemDark = window.matchMedia(
        "(prefers-color-scheme: dark)",
    ).matches;

    if (savedTheme === "dark" || (!savedTheme && systemDark)) {
        html.classList.add("dark");
        sunIcon?.classList.remove("hidden");
        moonIcon?.classList.add("hidden");
    } else {
        html.classList.remove("dark");
        sunIcon?.classList.add("hidden");
        moonIcon?.classList.remove("hidden");
    }

    themeBtn?.addEventListener("click", () => {
        html.classList.toggle("dark");
        const isDark = html.classList.contains("dark");
        localStorage.setItem("theme", isDark ? "dark" : "light");
        if (isDark) {
            sunIcon?.classList.remove("hidden");
            moonIcon?.classList.add("hidden");
        } else {
            sunIcon?.classList.add("hidden");
            moonIcon?.classList.remove("hidden");
        }
    });

    // Mobile Navigation
    const mobileFilesBtn = document.getElementById("mobile-files-btn");
    const mobileFileMenu = document.getElementById("mobile-file-menu");
    mobileFilesBtn?.addEventListener("click", () => {
        mobileFileMenu?.classList.remove("hidden");
        setTimeout(() => mobileFileMenu?.classList.add("opacity-100"), 10);
    });
    document
        .getElementById("mobile-theme-btn")
        ?.addEventListener("click", () => themeBtn?.click());
</script>

<style>
    #project-content::-webkit-scrollbar {
        width: 12px;
    }
    #project-content::-webkit-scrollbar-track {
        background: transparent;
    }
    #project-content::-webkit-scrollbar-thumb {
        background-color: #d1d5db;
        border-radius: 6px;
        border: 3px solid transparent;
        background-clip: content-box;
    }
    :global(.dark) #project-content::-webkit-scrollbar-thumb {
        background-color: #30363d;
    }
</style>
