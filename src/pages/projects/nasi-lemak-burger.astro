---
import Layout from "../../layouts/Layout.astro";
import IdeActivityBar from "../../components/ide/IdeActivityBar.astro";
import IdeSidebar from "../../components/ide/IdeSidebar.astro";
import MobileNav from "../../components/ide/MobileNav.astro";
import NotebookCell from "../../components/NotebookCell.astro";

const project = {
    title: "Nasi Lemak Burger - Promotional Landing",
    type: "Landing Page",
    description:
        "A high-converting promotional landing page designed to create urgency and drive immediate customer action through dynamic countdown timers.",
    image: "assets/burger.webp",
    liveUrl: "https://amiqt.github.io/landing_page_2/",
    duration: "1-3 months",
    role: "Solo Developer",
};

const technologies = [
    { name: "HTML5", color: "blue" },
    { name: "CSS3", color: "yellow" },
    { name: "JavaScript", color: "orange" },
];

const features = [
    {
        title: "Countdown Timer",
        icon: "⏱️",
        desc: "JavaScript-powered countdown timer that resets on each visit, creating perpetual urgency.",
    },
    {
        title: "Conversion Optimization",
        icon: "📈",
        desc: "Strategic CTAs and compelling copy designed to maximize conversion rates.",
    },
    {
        title: "Responsive Design",
        icon: "📱",
        desc: "Mobile-first design crucial for food delivery customers.",
    },
    {
        title: "User Psychology",
        icon: "🧠",
        desc: "Incorporates scarcity, urgency, and social proof triggers.",
    },
];

const files = [
    { name: "nasi-lemak-burger.html", active: true, icon: "🍔" },
    { name: "projects.json", active: false, icon: "📦" },
    { name: "home-software-developer.ipynb", active: false, icon: "📝" },
];
---

<Layout
    title="Nasi Lemak Burger - Promotional Landing | Noor Azami"
    description={project.description}
>
    <div
        class="flex h-screen bg-white dark:bg-[#0d1117] text-gray-900 dark:text-gray-300 overflow-hidden font-sans transition-colors duration-300"
    >
        <IdeActivityBar />
        <IdeSidebar files={files} />

        <main
            class="flex-1 flex flex-col min-w-0 bg-white dark:bg-[#0d1117] transition-colors duration-300"
        >
            <div
                class="flex bg-gray-100 dark:bg-[#010409] border-b border-gray-200 dark:border-gray-800 overflow-x-auto scrollbar-hide transition-colors duration-300"
            >
                <div
                    class="flex items-center gap-2 px-4 py-2 bg-white dark:bg-[#0d1117] border-t-2 border-orange-500 text-gray-900 dark:text-white text-sm min-w-fit cursor-pointer transition-colors duration-300"
                >
                    <span>🍔</span><span>nasi-lemak-burger.html</span><span
                        class="ml-2 text-gray-500 cursor-pointer">×</span
                    >
                </div>
                <a
                    href={import.meta.env.BASE_URL +
                        "role-software-developer/projects"}
                    class="flex items-center gap-2 px-4 py-2 text-gray-500 hover:bg-gray-200 dark:hover:bg-[#161b22] cursor-pointer text-sm min-w-fit transition-colors duration-300"
                >
                    <span>📦</span><span>projects.json</span>
                </a>
            </div>

            <div
                class="flex items-center gap-2 px-4 py-1 bg-white dark:bg-[#0d1117] border-b border-gray-200 dark:border-gray-800 text-xs text-gray-500 transition-colors duration-300"
            >
                <span>src</span><span>›</span><span>projects</span><span>›</span
                ><span>nasi-lemak-burger.html</span>
                <div class="ml-auto">
                    <span
                        class="px-2 py-0.5 bg-orange-100 dark:bg-orange-900/30 text-orange-700 dark:text-orange-300 rounded text-xs"
                        >Promo</span>
                </div>
            </div>

            <div
                class="flex-1 overflow-y-auto p-4 md:p-8 scroll-smooth"
                id="project-content"
            >
                <NotebookCell
                    cellNumber={1}
                    code={`// Project: Nasi Lemak Burger Promo\nconst promo = {\n  name: "${project.title}",\n  countdown: true,\n  urgency: "HIGH"\n};`}
                >
                    <section
                        class="relative flex flex-col items-center justify-center text-center py-12 md:py-16 bg-gradient-to-br from-orange-900 to-slate-900 rounded-lg overflow-hidden border border-orange-500/30 shadow-2xl"
                    >
                        <div class="z-10 px-4">
                            <div class="flex justify-center mb-4">
                                <span
                                    class="px-4 py-1 bg-orange-500/20 border border-orange-500/30 rounded-full text-sm text-orange-300"
                                    >Landing Page</span
                                >
                            </div>
                            <h1
                                class="text-3xl md:text-4xl font-bold mb-4 leading-tight text-white drop-shadow-lg"
                            >
                                {project.title}
                            </h1>
                            <p
                                class="text-base md:text-lg font-medium mb-6 max-w-2xl mx-auto leading-relaxed text-gray-200"
                            >
                                {project.description}
                            </p>
                            <div
                                class="flex flex-wrap justify-center gap-2 mb-6"
                            >
                                {
                                    technologies.map((tech) => (
                                        <span class="px-3 py-1 bg-black/30 backdrop-blur-md border border-white/20 rounded-full text-xs text-white">
                                            {tech.name}
                                        </span>
                                    ))
                                }
                            </div>
                            <a
                                href={project.liveUrl}
                                target="_blank"
                                rel="noopener noreferrer"
                                class="inline-flex items-center gap-2 px-6 py-3 bg-orange-600 hover:bg-orange-700 text-white rounded-lg font-medium transition-colors"
                            >
                                <svg
                                    class="w-5 h-5"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                    ><path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"
                                    ></path></svg
                                >
                                View Live Project
                            </a>
                        </div>
                    </section>
                </NotebookCell>

                <NotebookCell
                    cellNumber={2}
                    code={`// Load preview\nawait promo.loadPreview();`}
                >
                    <div
                        class="rounded-lg overflow-hidden border border-gray-200 dark:border-gray-700 shadow-xl"
                    >
                        <img
                            src={import.meta.env.BASE_URL + project.image}
                            alt={project.title}
                            class="w-full h-auto object-cover"
                            loading="lazy"
                        />
                    </div>
                </NotebookCell>

                <NotebookCell
                    cellNumber={3}
                    code={`// Display key features\nfeatures.forEach(f => console.log(f.title));`}
                >
                    <div>
                        <h2
                            class="text-2xl font-bold text-gray-900 dark:text-white text-center mb-6"
                        >
                            Key Features
                        </h2>
                        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
                            {
                                features.map((feature) => (
                                    <div class="bg-white dark:bg-[#161b22] p-5 rounded-lg border border-gray-200 dark:border-gray-700 hover:border-orange-500/50 transition-all">
                                        <div class="flex items-start gap-3">
                                            <div class="w-10 h-10 bg-orange-500/10 dark:bg-orange-500/20 rounded-lg flex items-center justify-center flex-shrink-0">
                                                <span class="text-xl">
                                                    {feature.icon}
                                                </span>
                                            </div>
                                            <div>
                                                <h3 class="font-semibold text-gray-900 dark:text-white mb-1">
                                                    {feature.title}
                                                </h3>
                                                <p class="text-sm text-gray-600 dark:text-gray-400">
                                                    {feature.desc}
                                                </p>
                                            </div>
                                        </div>
                                    </div>
                                ))
                            }
                        </div>
                    </div>
                </NotebookCell>

                <div class="mt-8 text-center">
                    <a
                        href={import.meta.env.BASE_URL +
                            "role-software-developer/projects"}
                        class="inline-flex items-center gap-2 px-6 py-3 bg-gray-100 dark:bg-[#161b22] border border-gray-200 dark:border-gray-700 rounded-lg text-sm font-medium text-gray-700 dark:text-gray-300 hover:border-orange-500/50 hover:text-orange-600 dark:hover:text-orange-400 transition-all"
                    >
                        <span>←</span> Back to All Projects
                    </a>
                </div>
            </div>
        </main>
        <MobileNav files={files} />
    </div>
</Layout>

<script>
    const themeBtn = document.getElementById("theme-toggle-btn");
    const sunIcon = document.getElementById("sun-icon");
    const moonIcon = document.getElementById("moon-icon");
    const html = document.documentElement;
    const savedTheme = localStorage.getItem("theme");
    const systemDark = window.matchMedia(
        "(prefers-color-scheme: dark)",
    ).matches;
    if (savedTheme === "dark" || (!savedTheme && systemDark)) {
        html.classList.add("dark");
        sunIcon?.classList.remove("hidden");
        moonIcon?.classList.add("hidden");
    } else {
        html.classList.remove("dark");
        sunIcon?.classList.add("hidden");
        moonIcon?.classList.remove("hidden");
    }
    themeBtn?.addEventListener("click", () => {
        html.classList.toggle("dark");
        const isDark = html.classList.contains("dark");
        localStorage.setItem("theme", isDark ? "dark" : "light");
        if (isDark) {
            sunIcon?.classList.remove("hidden");
            moonIcon?.classList.add("hidden");
        } else {
            sunIcon?.classList.add("hidden");
            moonIcon?.classList.remove("hidden");
        }
    });
    document
        .getElementById("mobile-files-btn")
        ?.addEventListener("click", () => {
            document
                .getElementById("mobile-file-menu")
                ?.classList.remove("hidden");
        });
    document
        .getElementById("mobile-theme-btn")
        ?.addEventListener("click", () => themeBtn?.click());
</script>

<style>
    #project-content::-webkit-scrollbar {
        width: 12px;
    }
    #project-content::-webkit-scrollbar-track {
        background: transparent;
    }
    #project-content::-webkit-scrollbar-thumb {
        background-color: #d1d5db;
        border-radius: 6px;
        border: 3px solid transparent;
        background-clip: content-box;
    }
    :global(.dark) #project-content::-webkit-scrollbar-thumb {
        background-color: #30363d;
    }
</style>
